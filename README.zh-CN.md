# Auto Sleeper

当你的 Windows 电脑空闲达到设定时长后自动进入睡眠的托盘小工具。基于 **Tauri v2**。

[English](./README.md) | 简体中文

## ✨ 功能

- **托盘常驻**：主窗口默认隐藏，主要通过托盘菜单交互。
- **空闲检测**：基于 Windows API 获取最后输入时间，计算空闲秒数。
- **自动休眠**：空闲超过设定阈值后触发系统睡眠。
- **阈值切换**：支持 15 / 30 / 60 分钟。
- **开机自启动**：集成 `tauri-plugin-autostart`（桌面端）。

## 🚀 快速开始（开发）

```bash
pnpm install
pnpm tauri dev
```

说明：`src-tauri/tauri.conf.json` 配置了 `beforeDevCommand: "pnpm dev"`，因此 `pnpm tauri dev` 会自动拉起前端 dev server。

## 📦 构建与打包

```bash
pnpm tauri build
```

## 🧰 技术栈

- **Tauri**：v2
- **前端**：Vite + Vanilla TypeScript
- **Rust**：Tokio 定时任务 + `windows-sys`
- **插件**：`tauri-plugin-autostart`、`tauri-plugin-opener`

## ⚙️ 配置说明

- **默认隐藏窗口**：`src-tauri/tauri.conf.json` 中 `visible: false`。
- **Capabilities**：如果你在前端调用 `@tauri-apps/plugin-autostart` 的 `enable/isEnabled/disable`，需要在 capability 配置中放行对应权限（Tauri v2 安全机制）。

## 🤖 AI 生成/辅助说明

本项目包含 **AI 辅助生成** 的代码与文档内容。请在使用/发布前自行审查关键逻辑（尤其是睡眠、权限、自启动相关行为），并按你的安全与合规要求进行必要调整。

## 🤝 贡献

欢迎提交 Issue / PR。

- **Bug**：请附上系统版本、复现步骤、相关日志/截图。
- **PR**：请说明改动动机与测试方式。

## 📄 许可证

MIT License，见 `LICENSE`。
